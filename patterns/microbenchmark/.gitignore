# Build artifacts (see int8dot_ceiling.c's header comment for build commands,
# or just `make run` / `make run TOOLCHAIN_ROOT=../script/clang_wrapper/`) --
# all four probes' .o/binaries/m5out* land entirely under these four dirs.
int8dot_ceiling/
fc_bottleneck/
int8dot_correctness_probe/
requant_correctness_probe/
